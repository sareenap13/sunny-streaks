// Copy this file to firebase-config.js and fill in your own Firebase project's
// values (Firebase console → Project settings → General → Your apps → SDK config).
// firebase-config.js is gitignored so real keys never get committed.

export const firebaseConfig = {
  apiKey: "PASTE_ME",
  authDomain: "PASTE_ME",
  projectId: "PASTE_ME",
  storageBucket: "PASTE_ME",
  messagingSenderId: "PASTE_ME",
  appId: "PASTE_ME",
};
